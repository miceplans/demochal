import { jsonb, pgTable, text, timestamp, uuid, varchar } from 'drizzle-orm/pg-core';
import { users } from './core.schema.js';
export const notifications=pgTable('notifications',{id:uuid('id').defaultRandom().primaryKey(),userId:uuid('user_id').notNull().references(()=>users.id),type:varchar('type',{length:50}).notNull(),payload:jsonb('payload').notNull(),readAt:timestamp('read_at'),createdAt:timestamp('created_at').defaultNow().notNull()});
export const operationInquiries=pgTable('operation_inquiries',{id:uuid('id').defaultRandom().primaryKey(),name:varchar('name',{length:100}).notNull(),contact:varchar('contact',{length:200}).notNull(),content:text('content').notNull(),createdAt:timestamp('created_at').defaultNow().notNull()});
export const certificates=pgTable('certificates',{id:uuid('id').defaultRandom().primaryKey(),userId:uuid('user_id').notNull().references(()=>users.id),award:varchar('award',{length:200}).notNull(),category:varchar('category',{length:20}).notNull(),fileId:uuid('file_id').notNull(),status:varchar('status',{length:20}).notNull().default('pending'),createdAt:timestamp('created_at').defaultNow().notNull()});
export const reports=pgTable('reports',{id:uuid('id').defaultRandom().primaryKey(),content:varchar('content',{length:200}).notNull(),targetType:varchar('target_type',{length:20}).notNull(),targetId:uuid('target_id'),org:varchar('org',{length:200}),summary:varchar('summary',{length:200}).notNull(),detail:text('detail'),reporterUserId:uuid('reporter_user_id').references(()=>users.id),reportedUserId:uuid('reported_user_id').references(()=>users.id),status:varchar('status',{length:20}).notNull().default('open'),resolutionNote:text('resolution_note'),reportedAt:timestamp('reported_at').defaultNow().notNull()});
export const adminSettings=pgTable('admin_settings',{id:varchar('id',{length:20}).primaryKey(),values:jsonb('values').notNull().default({})});
